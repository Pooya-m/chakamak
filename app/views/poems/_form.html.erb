<%= form_for @poem do |f| %>
    <% if @poem.errors.any? %>
        <div id="errorExplanation">
          <h2><%= pluralize(@poem.errors.count, "error") %> prohibited
            this post from being saved:</h2>
          <ul>
            <% @poem.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= f.label :poet_name %><br>
      <%= f.text_field :poet_name%>
    </p>

    <p>
      <%= f.label :content %><br>
      <%= f.text_area :content %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>